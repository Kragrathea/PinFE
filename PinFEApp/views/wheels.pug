extends layout


block content
  h1= Wheels

  div(style='width:100%;height:100%')
    div(style:'float:left')
    each item in items
      div(style='width:15%;height:auto;max-height:25%;float:left;filter: drop-shadow(22px 22px 22px rgba(0,0,0,1.0));')
        img(src="/wheels/?image="+item.file, alt=item.name style="display:block;width:100%;height:auto;")
    //img.backglass(src=image style="position:absolute;left:0px;top:0px;width:100%;height:100%")
    //div.swiper-pagination
  script.

    /*var tables = !{JSON.stringify(tables)}*/
    
