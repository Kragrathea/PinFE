extends layout


block content
  .container
    .col-sm-5.align-middle
      form.dropzone(id="my-awesome-dropzone" method='POST' enctype="multipart/form-data" action='/install/upload?table=')
        | Drop files here.

  script.
    Dropzone.options.myAwesomeDropzone = {
      init: function() {
        this.on("addedfile", function(file) { /*alert("Added file."); */});
        this.on("sending", function(file, xhr, data) {
          console.log(file.fullPath)

          console.log(file.name)          
          // if file is actually a folder
          if(file.fullPath){
              data.append("fullPath", file.fullPath);
              }else{
                data.append("fullPath", file.name);  
              }
          });
        }
    };



  //- h1= Zips

  //- div(style='')
  //-   div(style:'float:left')
  //-   each item in items
  //-     //- div(style='width:15%;height:auto;max-height:25%;float:left;filter: drop-shadow(22px 22px 22px rgba(0,0,0,1.0));')
  //-     div(style='')=item
  //- script.

    /*var tables = !{JSON.stringify(tables)}*/
    
